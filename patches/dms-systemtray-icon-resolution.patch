--- a/quickshell/Modules/DankBar/Widgets/SystemTrayBar.qml
+++ b/quickshell/Modules/DankBar/Widgets/SystemTrayBar.qml
@@ -64,7 +64,12 @@
             }
             if (icon.startsWith("/") && !icon.startsWith("file://"))
                 return `file://${icon}`;
-            return icon;
+            if (icon.startsWith("file://") || icon.startsWith("image://"))
+                return icon;
+            const resolved = Paths.resolveIconUrl(icon);
+            if (resolved)
+                return resolved;
+            return icon;
         }
         return "";
     }
